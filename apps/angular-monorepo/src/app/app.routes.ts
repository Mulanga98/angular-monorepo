import { Route } from '@angular/router';
import { CrossBorderLetterComponent } from '@angular-monorepo/cross-border-letter'

export const appRoutes: Route[] = [
    {
        path: "",
        component: CrossBorderLetterComponent
    }
];
